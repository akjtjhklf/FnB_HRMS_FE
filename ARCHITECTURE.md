# HRMS Architecture Diagram

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         BROWSER                                  │
│                    http://localhost:3000                         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                   FRONTEND (Next.js)                             │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   App Router                             │   │
│  │  /login  /dashboard  /posts  /employees  /attendance    │   │
│  └──────────────────────┬──────────────────────────────────┘   │
│                         │                                        │
│                         ▼                                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                 Feature Modules                          │   │
│  │  - Auth  - Dashboard  - Posts  - Employees  - etc       │   │
│  └──────────────────────┬──────────────────────────────────┘   │
│                         │                                        │
│                         ▼                                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Refine Core                                 │   │
│  │  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐  │   │
│  │  │AuthProvider │  │DataProvider  │  │RouterProvider│  │   │
│  │  └─────────────┘  └──────────────┘  └──────────────┘  │   │
│  └──────────────────────┬──────────────────────────────────┘   │
│                         │                                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Zustand Store (RBAC)                        │   │
│  │  - User  - Roles  - Permissions  - Policies             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
└───────────────────────────┬─────────────────────────────────────┘
                            │ HTTP/REST
                            │ axios
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                 BACKEND (Express.js)                             │
│                  http://localhost:3001                           │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    API Routes                            │   │
│  │  /api/auth/login                                         │   │
│  │  /api/posts  /api/employees  /api/attendance            │   │
│  └──────────────────────┬──────────────────────────────────┘   │
│                         │                                        │
│                         ▼                                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │               Business Logic                             │   │
│  │  - Controllers  - Services  - Repositories               │   │
│  └──────────────────────┬──────────────────────────────────┘   │
│                         │                                        │
│                         ▼                                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Directus SDK                                │   │
│  └──────────────────────┬──────────────────────────────────┘   │
│                         │                                        │
└─────────────────────────┼─────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                 DIRECTUS (Headless CMS)                          │
│                  http://localhost:8055                           │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Directus Core                               │   │
│  │  - Collections  - Items  - Files  - Users               │   │
│  │  - Roles  - Permissions  - Policies                     │   │
│  └──────────────────────┬──────────────────────────────────┘   │
│                         │                                        │
└─────────────────────────┼─────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE (MySQL)                              │
│  - users  - employees  - attendance_logs  - rfid_cards          │
│  - positions  - salary_schemes  - devices  - etc                │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 Frontend Component Hierarchy

```
App (layout.tsx)
│
├── Refine Provider
│   ├── AuthProvider
│   ├── DataProvider
│   └── RouterProvider
│
├── AntD Registry
│   └── Theme Configuration
│
└── Pages (App Router)
    │
    ├── /login
    │   └── LoginPage (features/auth)
    │       ├── Form
    │       └── useLogin (Refine)
    │
    ├── / (dashboard)
    │   └── Dashboard (features/dashboard)
    │       ├── Stats Cards
    │       ├── Charts
    │       └── Framer Motion
    │
    └── /posts
        ├── /posts (List)
        │   └── PostList (features/posts)
        │       ├── Table (Ant Design)
        │       ├── useList (Refine)
        │       └── Actions (Edit, Delete)
        │
        ├── /posts/create
        │   └── PostCreate (features/posts)
        │       ├── Form (Ant Design)
        │       └── useCreate (Refine)
        │
        ├── /posts/:id
        │   └── PostShow (features/posts)
        │       ├── Card (UI Component)
        │       └── useOne (Refine)
        │
        └── /posts/:id/edit
            └── PostEdit (features/posts)
                ├── Form (Ant Design)
                └── useUpdate (Refine)
```

## 🔄 Data Flow

### 1. Authentication Flow
```
User enters credentials
        ↓
    LoginPage
        ↓
useLogin (Refine hook)
        ↓
AuthProvider.login()
        ↓
POST /api/auth/login (Express)
        ↓
Directus Authentication
        ↓
← JWT Token + User Data
        ↓
Store in Zustand (authStore)
        ↓
Store in localStorage (persist)
        ↓
Redirect to Dashboard
```

### 2. Data Fetching Flow
```
Component mounts
        ↓
useList (Refine hook)
        ↓
DataProvider.getList()
        ↓
GET /api/posts?page=1&limit=10
        ↓
Express Route Handler
        ↓
Directus SDK query
        ↓
MySQL Database
        ↓
← JSON Response
        ↓
Transform data
        ↓
Update component state
        ↓
Render UI
```

### 3. Create/Update Flow
```
User submits form
        ↓
useCreate/useUpdate (Refine)
        ↓
DataProvider.create/update()
        ↓
POST/PATCH /api/posts
        ↓
Express validation
        ↓
Directus SDK mutation
        ↓
MySQL Database
        ↓
← Success response
        ↓
Show notification
        ↓
Redirect to list
```

## 🔐 RBAC Flow

```
User logs in
        ↓
Backend returns:
├── User data
├── Roles []
└── Permissions []
        ↓
Store in Zustand (authStore)
        ↓
Component checks access
        ↓
useAuthStore()
├── hasRole("admin")
├── hasPermission("create", "posts")
└── canAccess("posts", "delete")
        ↓
Render based on permissions
        ↓
Show/Hide UI elements
```

## 📦 Module Dependencies

```
app/
  ↓ imports
features/
  ↓ uses
components/ui/
  ↓ uses
lib/utils/
  ↓ imports
types/
```

```
features/
  ↓ uses
providers/ (Refine)
  ↓ calls
Backend API
  ↓ queries
Directus
  ↓ reads/writes
Database
```

```
store/ (Zustand)
  ↓ persists
localStorage
  ↓ syncs
features/
  ↓ uses
RBAC methods
```

## 🎨 Styling Stack

```
Component
    ↓
Ant Design Props
    ↓
className={cn(...)}
    ↓
Tailwind Classes
    ↓
    ├── Utility classes
    ├── Custom theme
    └── Responsive
    ↓
PostCSS
    ↓
Compiled CSS
    ↓
Browser
```

## 🚀 Build Process

```
Development:
next dev
  ↓
Fast Refresh
  ↓
Hot Module Replacement

Production:
next build
  ↓
TypeScript Check
  ↓
Optimize Components
  ↓
Generate Static Pages
  ↓
Bundle JavaScript
  ↓
Process CSS
  ↓
.next/ output
  ↓
next start
```

## 📱 Request/Response Cycle

```
Browser Request
    ↓
Next.js Server
    ↓
React Server Components
    ↓
Client Components
    ↓
Refine Hooks
    ↓
API Request (axios)
    ↓
Express Middleware
    ↓
Route Handler
    ↓
Directus SDK
    ↓
Database Query
    ↓
← Response
    ↓
Transform Data
    ↓
Update React State
    ↓
Re-render Components
    ↓
Browser Display
```

---

**This architecture ensures:**
- ✅ Separation of concerns
- ✅ Scalability
- ✅ Maintainability
- ✅ Type safety
- ✅ Performance
- ✅ Security (RBAC)
