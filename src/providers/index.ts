// Export authProvider từ auth-provider client (sử dụng tokenManager với ac_token/rf_token)
export { authProviderClient as authProvider } from "./auth-provider/auth-provider.client";

// Export dataProvider
export { dataProvider } from "./data-provider";
